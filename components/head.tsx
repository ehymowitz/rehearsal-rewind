import Head from "next/head";

const Header = () => {
  return (
    <Head>
      <title>Rehearsal Rewind</title>
      <meta name="description" content="A way to review your rehearsals" />
      <link rel="icon" href="/favicon.ico" />
    </Head>
  );
};

export default Header;
